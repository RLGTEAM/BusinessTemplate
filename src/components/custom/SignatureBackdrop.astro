---
/**
 * Decorative layer rendered inside every Hero variant, behind/around the hero
 * content — the second sanctioned creative mount point (docs/CREATIVE-CONTRACT.md).
 * Spinning vinyl, floating tools, parallax motifs live here.
 *
 * In the template this renders nothing. On a client site, return purely
 * decorative markup: aria-hidden, pointer-events-none, token colors only,
 * animated via data-reveal or src/components/custom/signature.ts.
 * The hero <section> is position:relative — absolute positioning works.
 */
---
